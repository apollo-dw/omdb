services:
  omdb:
    build: .
    ports:
      - "8400:80"
    env_file: .env
    volumes:
      - .:/var/www/html
    depends_on:
      - db

  db:
    image: mysql:8
    ports:
      - "127.0.0.1:3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: omdb
    volumes:
      - db:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  db:
